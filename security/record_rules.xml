<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <!-- Company-specific access rule for translation tasks -->
    <record id="translation_task_company_rule" model="ir.rule">
        <field name="name">Translation Task: Multi-Company</field>
        <field name="model_id" ref="model_sc_translation_task"/>
        <field name="domain_force">['|', ('company_id', '=', False), ('company_id', 'in', company_ids)]</field>
        <field name="groups" eval="[(4, ref('base.group_user'))]"/>
    </record>

    <!-- Users can only see their own translation tasks unless they are managers -->
    <record id="translation_task_user_rule" model="ir.rule">
        <field name="name">Translation Task: User Access</field>
        <field name="model_id" ref="model_sc_translation_task"/>
        <field name="groups" eval="[(4, ref('group_marketing_automation_user'))]"/>
        <field name="domain_force">[('create_uid', '=', user.id)]</field>
    </record>

    <!-- Managers can see all translation tasks -->
    <record id="translation_task_manager_rule" model="ir.rule">
        <field name="name">Translation Task: Manager Access</field>
        <field name="model_id" ref="model_sc_translation_task"/>
        <field name="groups" eval="[(4, ref('group_marketing_automation_manager'))]"/>
        <field name="domain_force">[(1, '=', 1)]</field>
    </record>
</odoo>
